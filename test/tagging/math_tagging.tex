\relax
\taggingactive=1


% math mode boundaries

A sample piece of text. We test an empty math expression here: $ $. And another one with a variable here: $x$.

% simple variables and operators

Let's test simple variables and operators. 
$x + y = z$. 
$1 + 2 = 3$. 
$-a$.

Here is an inline delimited expression: $\left( a^2 + n\right)$.

$$ a + b = c $$

% sub/super scripts

$x^2 + y_1 = z_i^k$.
$x^{n+1}$.
$a^{\alpha^\beta}_{2_b} F_1  {}_2 $.
$a_{b_c}$.
$a^{b_c}$.
$a_{b^c}$.


% fractions

${a \over b}$.
${1 \over x + {1 \over y}}$.

$${a \over b}  ={1 \over x + {1 \over y}}$$

% accents

Math accents $\hat a + \vec a \vec e + \tilde b + \hat{b} + \vec v$

Wide accents $\widehat{a+b} + \widetilde{x+y+z}$

% radicals and roots

$\sqrt{x^2 + y^2}$.
$\root 3 \of z$.
$\Uroot 0 "221A 3 x$.
$\root n \of {1+x^n}$.

% fenced and delimiters

$\left( {a \over b} \right)$.

% compound operators

$\sum_{i=0}^{\infty} x_i$.
$\int_a^b f(x) dx$.
$$\sum_{i=0}^{\infty} x_i$$

% general math constructs

$\pmatrix{a & b \cr c & d}$.
$\widehat{x+y}$.

% complex math expressions

$$ \int_0^\infty { \sqrt{x^2 + \widehat{y_i}^3} \over \left( \sum_{k=1}^n {a_k \over b_k} \right)^2 } \, dx = \pmatrix{ \alpha & \beta \cr \gamma & \delta } $$

$$ \pmatrix{ 
    a & b \cr
    c & d } $$

$$ \pmatrix{ 
   \int_a^b f(x) dx & \sqrt{ {x_1^2 + y_1^2} \over z_i } \cr
   \sum_{i=1}^\infty \widehat{a_i} & {1 \over 1 + {1 \over x}} } 
   $$
\bye

